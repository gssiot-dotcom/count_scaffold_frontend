version: '3.8'

services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL}
        VITE_AI_BASE_URL: ${VITE_AI_BASE_URL}
    container_name: count_frontend
    ports:
      - "3003:80"
    restart: always
